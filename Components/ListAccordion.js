import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ListAccordion = ({value,index}) => {
  return (
    <View>
      <Text>ListAccordion</Text>
    </View>
  )
}

export default ListAccordion

const styles = StyleSheet.create({})